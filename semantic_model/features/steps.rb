# encoding: utf-8

Given /^an order is in state "(.*?)"$/ do |current_state|
  pending # express the regexp above with the code you wish you had
end

When /^the event "(.*?)" has occurred$/ do |event|
  pending # express the regexp above with the code you wish you had
end

Then /^the order will be in state "(.*?)"$/ do |next_state|
  pending # express the regexp above with the code you wish you had
end
